# JazzLight – kompletní opravená verze

Toto je celý projekt, ne patch.

## Instalace

```bash
npm install
npx vercel dev
```

## Stránky

- `/`
- `/admin`

## ENV

Použij `.env.local` nebo Vercel env:

```env
ADMIN_TOKEN=...
FIREBASE_DATABASE_URL=...
FIREBASE_SERVICE_ACCOUNT_BASE64=...
```
